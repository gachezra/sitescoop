export type Product = {
  id: string;
  name: string;
  price: number;
  rating: number;
  imageUrl: string;
};

export type SelectorMap = {
  container: string;
  name: string;
  price: string;
  rating: string;
  imageUrl: string;
};
