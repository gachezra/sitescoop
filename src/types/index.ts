export type Product = {
  id: string;
  title: string;
  description: string;
  link: string;
  imageUrl: string;
  date?: string;
};

export type SelectorMap = {
  container: string;
  title: string;
  description: string;
  link: string;
  imageUrl: string;
};
