export type Product = {
  id: string;
  title: string;
  description: string;
  link: string;
  imageUrl: string;
};

export type SelectorMap = {
  container: string;
  title: string;
  description: string;
  link: string;
  imageUrl: string;
};
